{
  "apiRequestTemplate": {
    "application/json": {
      "totalvalue": "$input.params('totalvalue')",
      "word": "$input.params('word')",
      "count": "$input.params('count')",
      "productname": "$input.params('productname')"
    }
  }
}